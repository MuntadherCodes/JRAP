/**
 * JRAP AI gateway: single LLM entry point - provider adapters, prompt registry, budgets (Phase 4: CON-4, NFR-AI-1)
 *
 * <p>Module stub — implemented in a later phase per the phased implementation plan.
 * Module boundaries are fixed now so later phases plug in without re-architecture
 * (SRS NFR-MNT-1).</p>
 */
package dev.hmcodes.jrap.aigateway;
